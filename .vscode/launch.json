{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Current Folder Executable",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Consensus/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/Consensus",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
